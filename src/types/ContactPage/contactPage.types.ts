export interface ContactBoxPatternProps {
  image: string;
  title: string;
  description: string;
  times?: string;
  days?: string;
}

export interface AccordionAssistsProps {
    title: string;
    children: React.ReactNode;
  }